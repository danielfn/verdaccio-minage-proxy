# FORMAT EXAMPLE ONLY — contains NO real, usable credential.
# Do NOT copy as-is (nobody could authenticate with this hash). Create real
# accounts with:  ./scripts/generate-htpasswd.sh <user> '<password>' > conf/htpasswd
svc-example:$2a$10$...example-bcrypt-hash-not-real...
